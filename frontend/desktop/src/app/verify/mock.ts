export const verificationInfo = {
  codeLength: 6,
  /** Kodun geçerlilik süresi. */
  ttlMinutes: 10,
  /** Backend'in izin verdiği en fazla deneme sayısı. */
  attemptLimit: 5,
  /** Kod yeniden gönderilmeden önce beklenecek süre. */
  resendSeconds: 60,
};
